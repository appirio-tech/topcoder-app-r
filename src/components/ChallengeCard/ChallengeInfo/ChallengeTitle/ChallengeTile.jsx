import React, { PropTypes } from 'react'

require('./ChallengeTile.scss')

const ChallengeTile = ({ /* Add destructured props here */ }) => {
}

ChallengeTile.propTypes = {
  // Add all proptypes
}

export default ChallengeTile

import React, { PropTypes } from 'react'

require('./ChallengeActivity.scss')

const ChallengeActivity = ({ /* Add destructured props here */ }) => {
}

ChallengeActivity.propTypes = {
  // Add all proptypes
}

export default ChallengeActivity

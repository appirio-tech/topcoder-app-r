import React, { PropTypes } from 'react'

require('./CallToAction.scss')

const CallToAction = ({ /* Add destructured props here */ }) => {
}

CallToAction.propTypes = {
  // Add all proptypes
}

export default CallToAction
